package AttackLogic;

import Charactor.BasePlayer;

// Wind logic (x-oscillation + hitbox shift) is handled by Controller
public class WindAttack implements Attackable {
    @Override
    public void attack(BasePlayer enemy) {}
}
